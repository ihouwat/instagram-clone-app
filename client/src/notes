Header Searchbox to-dos:
- Grid display with username and full name displayed vertically
- Horizontal spacing of the <li> child items
- 'Nothing' <li> use-case displays in the middle of the <ul> container 
- Add MouseEvent listener for closing search box when user clicks mouse outside 

To dos found in:

- App routing module
  Remove 'user' from Users routing module
- All form validations
- Routable modal angular
- Eventually need http interceptor service for 
  404 routing
  jwt safety
  routing to home OR login page


- technologies added:
  ngx-pipes: https://www.npmjs.com/package/ngx-pipes#timeago